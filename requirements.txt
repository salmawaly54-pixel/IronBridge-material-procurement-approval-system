mcp==1.28.1
# CRITICAL: pinned exactly. mcp 2.0.0 (the current latest) removed
# mcp.server.fastmcp and restructured the client-side callback API
# (ElicitResult.content vs .data, RequestContext naming, etc). server.py
# and agent/ are written against 1.28.1's API and will not import under
# 2.0.0 -- verified by installing both during development.
starlette>=1.3.1
uvicorn>=0.51.0
pydantic>=2.13.4
anyio>=4.14.2
python-dotenv>=1.0.1

# Optional -- only needed if IRONBRIDGE_DB_ENGINE=mssql (see .env.example
# and db/mssql/). Also requires a system ODBC driver, not installable via pip.
# pyodbc>=5.1
